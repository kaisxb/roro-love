<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>رسالة خاصة من القلب ❤️</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #3a1024, #12040c);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      position: relative;
      color: #ffffff;
      padding: 20px;
    }

    /* خلفية القلوب المتساقطة */
    .heart-bg {
      position: absolute;
      top: -10%;
      font-size: 20px;
      animation: fall linear infinite;
      user-select: none;
      pointer-events: none;
      opacity: 0.7;
    }

    @keyframes fall {
      0% {
        transform: translateY(0) rotate(0deg);
        opacity: 0.9;
      }
      100% {
        transform: translateY(110vh) rotate(360deg);
        opacity: 0;
      }
    }

    /* كرت الرسالة الرئيسي */
    .card {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      border: 1px solid rgba(255, 255, 255, 0.18);
      border-radius: 24px;
      padding: 35px 25px;
      max-width: 480px;
      width: 100%;
      text-align: center;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
      z-index: 10;
      transition: all 0.4s ease;
    }

    .heart-icon {
      font-size: 50px;
      display: inline-block;
      animation: pulse 1.6s infinite ease-in-out;
      margin-bottom: 15px;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.22); }
    }

    h1 {
      font-size: 26px;
      color: #ff7597;
      margin-bottom: 15px;
      font-weight: 700;
    }

    .intro-text {
      font-size: 16px;
      color: #f1d5df;
      line-height: 1.8;
      margin-bottom: 25px;
    }

    .btn {
      background: linear-gradient(135deg, #ff4071, #ff7597);
      border: none;
      color: #fff;
      padding: 13px 32px;
      font-size: 16px;
      font-weight: bold;
      border-radius: 50px;
      cursor: pointer;
      box-shadow: 0 8px 20px rgba(255, 64, 113, 0.4);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 25px rgba(255, 64, 113, 0.55);
    }

    .btn:active {
      transform: scale(0.97);
    }

    /* محتوى الرسالة بعد الضغط على الزر */
    .secret-letter {
      display: none;
      margin-top: 25px;
      border-top: 1px dashed rgba(255, 255, 255, 0.25);
      padding-top: 20px;
      animation: fadeIn 0.8s ease forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .message {
      font-size: 17px;
      line-height: 2;
      color: #ffe6ee;
      white-space: pre-line;
      background: rgba(0, 0, 0, 0.2);
      padding: 18px;
      border-radius: 16px;
      border: 1px solid rgba(255, 117, 151, 0.25);
    }

    .signature {
      margin-top: 15px;
      font-size: 15px;
      color: #ff94b1;
      font-weight: 600;
    }
  </style>
</head>
<body>

  <!-- حاوية القلوب المتحركة -->
  <div id="hearts-container"></div>

  <!-- الكرت -->
  <div class="card">
    <div class="heart-icon">💖</div>
    <h1>إليك، بكل ما في قلبي</h1>
    <p class="intro-text">هناك كلمات لا تتسع لها الأحاديث العادية، فصنعت لك هذه المساحة الصغيرة لأعبر لك عنها...</p>
    
    <button class="btn" id="openBtn" onclick="revealLetter()">افتح الرسالة 💌</button>

    <div class="secret-letter" id="secretLetter">
      <div class="message">
        وجودك في حياتي مثل النور في وسط العتمة. 
        كل يوم يمضي وأنت بقربي يزيدني اطمئنان وراحة ما لقيتها في أي مكان ثاني.
        
        شكراً لأنك دائماً ترسم البسمة على وجهي، وشكراً على كل لحظة حلوة وجودك فيها يخليها أثمن وأجمل.
        
        أحبك بكل تفاصيلك وبكل ما أوتيت من شعور ❤️
      </div>
      <div class="signature">✨ دائماً وأبداً في قلبي ✨</div>
    </div>
  </div>

  <script>
    // إنشاء تأثير القلوب المتساقطة تلقائياً
    const icons = ['❤️', '💖', '✨', '🌸', '💕'];
    const container = document.getElementById('hearts-container');

    function createHeart() {
      const heart = document.createElement('div');
      heart.classList.add('heart-bg');
      heart.innerText = icons[Math.floor(Math.random() * icons.length)];
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = (Math.random() * 3 + 3) + 's';
      heart.style.fontSize = (Math.random() * 18 + 14) + 'px';
      container.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 6000);
    }

    setInterval(createHeart, 350);

    // فتح الرسالة عند الضغط
    function revealLetter() {
      const letter = document.getElementById('secretLetter');
      const btn = document.getElementById('openBtn');
      
      letter.style.display = 'block';
      btn.style.display = 'none';

      // إطلاق دفعة قلوب إضافية احتفالاً بالفتح
      for (let i = 0; i < 20; i++) {
        setTimeout(createHeart, i * 80);
      }
    }
  </script>
</body>
</html>
