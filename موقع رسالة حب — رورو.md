```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>إلى رورو ❤️</title>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      overflow: hidden;
      font-family: "Segoe UI", Tahoma, Arial, sans-serif;
      background:
        radial-gradient(circle at 20% 20%, rgba(255, 100, 140, .18), transparent 30%),
        radial-gradient(circle at 80% 80%, rgba(255, 50, 100, .15), transparent 30%),
        linear-gradient(135deg, #090006, #210611, #090006);
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    /* النجوم */
    .stars {
      position: fixed;
      inset: 0;
      pointer-events: none;
    }

    .star {
      position: absolute;
      width: 3px;
      height: 3px;
      background: white;
      border-radius: 50%;
      opacity: .5;
      animation: twinkle 3s infinite ease-in-out;
    }

    @keyframes twinkle {
      0%, 100% { opacity: .15; transform: scale(.7); }
      50% { opacity: .9; transform: scale(1.4); }
    }

    /* القلوب */
    .heart {
      position: fixed;
      bottom: -50px;
      font-size: 20px;
      opacity: .7;
      pointer-events: none;
      animation: floatHeart linear forwards;
    }

    @keyframes floatHeart {
      0% {
        transform: translateY(0) scale(.7) rotate(0deg);
        opacity: 0;
      }
      15% {
        opacity: .8;
      }
      100% {
        transform: translateY(-115vh) scale(1.4) rotate(25deg);
        opacity: 0;
      }
    }

    /* البطاقة */
    .card {
      width: min(90%, 560px);
      padding: 55px 35px;
      text-align: center;
      border: 1px solid rgba(255,255,255,.14);
      border-radius: 30px;
      background: rgba(255,255,255,.07);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      box-shadow:
        0 30px 80px rgba(0,0,0,.55),
        0 0 60px rgba(255, 40, 100, .10);
      position: relative;
      z-index: 5;
      animation: cardIn 1.2s ease;
    }

    @keyframes cardIn {
      from {
        opacity: 0;
        transform: translateY(35px) scale(.95);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    .small {
      font-size: 15px;
      color: rgba(255,255,255,.65);
      letter-spacing: 2px;
      margin-bottom: 18px;
    }

    .name {
      font-size: clamp(45px, 12vw, 75px);
      font-weight: 800;
      margin-bottom: 10px;
      background: linear-gradient(90deg, #fff, #ffb4c7, #fff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 0 0 35px rgba(255, 100, 150, .25);
    }

    .subtitle {
      color: rgba(255,255,255,.72);
      font-size: 17px;
      margin-bottom: 35px;
    }

    button {
      border: none;
      cursor: pointer;
      padding: 16px 35px;
      border-radius: 999px;
      color: white;
      font-size: 17px;
      font-weight: bold;
      background: linear-gradient(135deg, #ff315f, #b90038);
      box-shadow:
        0 10px 30px rgba(255, 20, 80, .3),
        inset 0 1px 1px rgba(255,255,255,.25);
      transition: .3s ease;
    }

    button:hover {
      transform: translateY(-3px) scale(1.03);
      box-shadow:
        0 15px 40px rgba(255, 20, 80, .45);
    }

    button:active {
      transform: scale(.97);
    }

    /* الرسالة */
    .message {
      display: none;
      animation: messageIn 1.2s ease;
    }

    @keyframes messageIn {
      from {
        opacity: 0;
        transform: scale(.85);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .big-heart {
      font-size: 65px;
      margin-bottom: 20px;
      animation: heartbeat 1.3s infinite;
      filter: drop-shadow(0 0 18px rgba(255,40,90,.6));
    }

    @keyframes heartbeat {
      0%, 100% { transform: scale(1); }
      15% { transform: scale(1.18); }
      30% { transform: scale(1); }
      45% { transform: scale(1.12); }
    }

    .message h2 {
      font-size: clamp(30px, 8vw, 48px);
      margin-bottom: 20px;
    }

    .message p {
      font-size: clamp(20px, 5vw, 27px);
      line-height: 2;
      color: #ffe5ec;
      font-weight: 500;
    }

    .signature {
      margin-top: 25px;
      color: rgba(255,255,255,.55);
      font-size: 14px;
    }

    /* تأثير اللمعة */
    .glow {
      position: absolute;
      width: 250px;
      height: 250px;
      background: #ff174f;
      filter: blur(130px);
      opacity: .12;
      border-radius: 50%;
      pointer-events: none;
    }

    .glow.one {
      top: -100px;
      right: -100px;
    }

    .glow.two {
      bottom: -100px;
      left: -100px;
    }

    @media (max-width: 500px) {
      .card {
        padding: 45px 22px;
        border-radius: 25px;
      }

      .subtitle {
        font-size: 15px;
      }

      button {
        width: 90%;
      }
    }
  </style>
</head>

<body>

  <div class="stars" id="stars"></div>

  <div class="glow one"></div>
  <div class="glow two"></div>

  <main class="card">

    <!-- البداية -->
    <section id="start">
      <div class="small">رسالة خاصة جدًا إلى</div>

      <div class="name">رورو</div>

      <div class="subtitle">
        فيه شيء من قلبي أبي أقوله لك...
      </div>

      <button onclick="openMessage()">
        افتحي رسالتي ❤️
      </button>
    </section>

    <!-- الرسالة -->
    <section class="message" id="message">

      <div class="big-heart">❤️</div>

      <h2>رورو</h2>

      <p>
        أحبك،<br>
        الله لا يحرمني منك ❤️
      </p>

      <div class="signature">
        من شخص يحبك كثيرًا 🤍
      </div>

    </section>

  </main>

  <script>

    /* إنشاء النجوم */
    const stars = document.getElementById("stars");

    for (let i = 0; i < 70; i++) {
      const star = document.createElement("div");
      star.className = "star";

      star.style.left = Math.random() * 100 + "%";
      star.style.top = Math.random() * 100 + "%";
      star.style.animationDelay = Math.random() * 3 + "s";

      stars.appendChild(star);
    }

    /* فتح الرسالة */
    function openMessage() {
      document.getElementById("start").style.display = "none";
      document.getElementById("message").style.display = "block";

      createHearts();
    }

    /* إنشاء القلوب */
    function createHeart() {
      const heart = document.createElement("div");

      heart.className = "heart";

      const hearts = ["❤️", "💗", "💕", "💖", "💓"];
      heart.innerHTML = hearts[Math.floor(Math.random() * hearts.length)];

      heart.style.left = Math.random() * 100 + "vw";

      const size = 14 + Math.random() * 22;
      heart.style.fontSize = size + "px";

      const duration = 5 + Math.random() * 5;
      heart.style.animationDuration = duration + "s";

      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, duration * 1000);
    }

    function createHearts() {
      for (let i = 0; i < 18; i++) {
        setTimeout(createHeart, i * 120);
      }

      setInterval(createHeart, 650);
    }

  </script>

</body>
</html>
```